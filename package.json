{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "devDependencies": {
    "@babylonjs/core": "^5.37.0",
    "@babylonjs/inspector": "^5.37.0",
    "nuxt": "3.0.0",
    "push-dir": "^0.4.1",
    "sass": "^1.56.2"
  },
  "dependencies": {
    "babylonjs-loaders": "^5.37.0"
  }
}
